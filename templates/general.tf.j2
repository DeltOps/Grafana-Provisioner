terraform {
  required_providers {
    grafana = {
      source = "grafana/grafana"
      version = "~> 3.25.0"
    }
  }
}

provider "grafana" {
  url = "{{ grafana.url }}"
  auth = "{{ grafana.auth }}"
}
